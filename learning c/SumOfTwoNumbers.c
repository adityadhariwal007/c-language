#include <stdio.h>
int main()
{
    int x, y, sum;
    printf("enter your first no.");
    scanf("%d", &x);
    printf("enter your second no.");
    scanf("%d", &y);

    sum = (x + y);
        printf("sum of the no. is : %d", sum);

            return 0;
}